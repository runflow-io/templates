{
  "name": "judge-dashboard",
  "private": true,
  "version": "0.0.1",
  "description": "Contest judge dashboard for Runflow Builder Contest",
  "scripts": {
    "build": "mkdir -p dist && cp index.html submissions.json dist/"
  }
}
